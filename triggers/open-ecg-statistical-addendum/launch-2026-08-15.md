# TRUST-ECG statistical publication addendum launch

Launch the validated aggregate-only statistical addendum on the frozen TRUST-ECG v0.4 study state.

The workflow must:
- reproduce the deterministic fixed ResNet execution without changing scientific settings;
- compute 2,000-replicate paired bootstrap confidence intervals;
- compare the fixed ResNet with the locked handcrafted Logistic Regression reference;
- perform matched Phase-1 recalibration comparisons with multiplicity correction;
- generate aggregate calibration-bin tables and publication-quality PNG/PDF figures;
- publish no record-level predictions, logits, identifiers, waveforms, adaptation indices, calibration parameters, or checkpoints.
